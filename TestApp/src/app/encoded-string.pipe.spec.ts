import { EncodedStringPipe } from './encoded-string.pipe';

describe('EncodedStringPipe', () => {
  it('create an instance', () => {
    const pipe = new EncodedStringPipe();
    expect(pipe).toBeTruthy();
  });
});
